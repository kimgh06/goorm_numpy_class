# 텐서란?
ndarray는 텐서를 다루기 위한 오브젝트임.

텐서란?

스칼라는 하나의 값을 가지고 있음. 0차원 텐서,() 하나의 값을 가진 ndarray.

차원을 하나 넘으면 벡터 1차원 텐서, (m,) m개의 ndarray를 가지고 있음.

차원을 하나 더 넘으면 행렬 2차원 텐서(n, m) n개의 m ndarray를 가지고 있음. 

3차 텐서 (o,n,m)

전부 텐서, ndarray

텐서는 겉을 먼저 연산하고 안을 나중에 계산함. (o, n, m) o -> n -> m 순 계산

