# 정해진 간격과 개수로 ndarray 만들기

```python
import numpy as ap

list(range(10)) # 0...9
list(range(2,4)) # 2, 3
list(range(2, 8, 2)) # 2, 4, 6

np.arange(10) # 0...9
np.arange(2, 4) # 2, 3
np.arange(2, 8, 2) # 2, 4, 6
```